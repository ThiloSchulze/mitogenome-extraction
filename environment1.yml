# You can use this file to create a conda environment for this pipeline:
# conda env create -f environment1.yml

name: mitogenome-extraction
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - bioconda::seqkit=0.15.0
  - bioconda::blast=2.5.0
  - bioconda::novoplasty=4.3.1
  - bioconda::emboss=6.6.0